// /rocket/beans/rebind/aop/invoketarget/invoke-target-method.txt

// the overloaded asObject() takes the returned value and wraps it inside an object.
return chain.asObject( target.${method}( ${unwrapParameters} ));

