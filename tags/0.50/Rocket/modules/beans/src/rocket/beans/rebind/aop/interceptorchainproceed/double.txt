// /rocket/beans/rebind/aop/interceptorchainproceed/double.txt

// invokes the interceptor chain, unwrapping Double because the method return type is double
return chain.proceedReturningDouble();

