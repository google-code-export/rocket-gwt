// invoker.txt

// declare the invoker variable and create a new Post or Get invoker.
final ${invokerType} invoker = new ${invokerType}(){
    
protected java.lang.Object asObject( final com.google.gwt.json.client.JSONValue jsonValue ){
	final rocket.json.client.JsonSerializer deserializer =
		(rocket.json.client.JsonSerializer) 
		com.google.gwt.core.client.GWT.create( ${methodReturnType}.class );
    
    	return deserializer.asObject( jsonValue );
  	} // asObject
}; // ${invokerType}

${addParameters}

invoker.setUrl( this.getServiceEntryPoint());

// save the callback parameter
invoker.setCallback( ${callbackParameter} );

invoker.makeRequest( this );